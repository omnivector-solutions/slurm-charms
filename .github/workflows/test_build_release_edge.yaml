name: TestBuildDeployEdge

on: pull_request


jobs:
  lint-charms:
    name: "Testing"
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: "Test the system"
        run: |
          ip a
          hostname
